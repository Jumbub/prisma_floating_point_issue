datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
  output   = "./.generated"
}

model Test {
  id     Int     @id @default(autoincrement())
  single Decimal
  obj    Json
  arr    Json[]
}
